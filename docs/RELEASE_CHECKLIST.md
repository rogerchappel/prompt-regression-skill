# Release Checklist

- `npm run check` passes.
- `npm test` passes.
- `npm run smoke` passes.
- `npm run demo:failures` exits non-zero and shows useful failures.
- README documents the fixture format.
- `SKILL.md` states model-call and external-action boundaries.
